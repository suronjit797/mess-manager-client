import React from 'react';
import Layout from '../Components/Layout/Layout'

const StartNewMonth = () => {
    return (
        <Layout>
            Start new month
        </Layout>
    );
};

export default StartNewMonth;