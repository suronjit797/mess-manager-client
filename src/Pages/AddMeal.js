import React from 'react';
import Layout from '../Components/Layout/Layout'

const AddMeal = () => {
    return (
        <Layout>
            add meal
        </Layout>
    );
};

export default AddMeal;