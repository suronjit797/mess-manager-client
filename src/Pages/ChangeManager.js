import React from 'react';
import Layout from '../Components/Layout/Layout'

const ChangeManager = () => {
    return (
        <Layout>
            Change manager
        </Layout>
    );
};

export default ChangeManager;